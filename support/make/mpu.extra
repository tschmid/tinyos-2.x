# Set up flag for MPU protection to be used (conditional compilation)
# Put functions in sections of their own so that linker script can group and align them
PFLAGS += -DMPU_PROTECTION -ffunction-sections
